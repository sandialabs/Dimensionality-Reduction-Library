MiniMat = require("MiniMat");

// some filter functions
// for low variance filter
// for high coorelation filter


class DimRed {
    constructor(Mat) {
        this.Mat=Mat;
    }

    // filter
    // pca
    // backward feature elim
    // forward feature construction
}

module.exports = DimRed;
